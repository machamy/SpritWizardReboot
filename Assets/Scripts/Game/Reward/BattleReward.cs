using System.Collections.Generic;
using UnityEngine;

public class BattleReward : MonoBehaviour
{/*
    [SerializeField] private GameObject cardPrefab;


    public RewardType[] GetRewardType(SeedType seed)
    {
        return rewardChance[seed].GetRandomChoice().ToArray();
    }

    public Rarity GetNewCardRarity(SeedType seed)
    {
        return addCardWeightData[seed].GetRandomChoice();
    }

    public RewardAmountData GetRewardAmountData(SeedType seed)
    {
        return rewardAmountData[seed];
    }*/
}
